# 🎮 Интерактивный туториал - Руководство

## ✅ Что было добавлено

### 1. Интерактивный туториал с подсветкой элементов
- 15 шагов обучения
- Автоматическое переключение между страницами
- Подсветка кнопок и элементов
- Невозможно пропустить - обязательное прохождение

### 2. Стартовые юниты для новых пользователей
- Freddy (ID: 101)
- Bonnie (ID: 103)
- Chica (ID: 102)

Выдаются автоматически при первом входе.

### 3. Порядок показа функций

**Main (2 шага):**
1. Приветствие в главном меню
2. Сообщение о получении стартовых юнитов

**Casino (3 шага):**
3. Подсветка кнопки Casino в навбаре
4. Объяснение казино
5. Обязательный спин колеса (пользователь должен нажать)

**More Menu (1 шаг):**
6. Подсветка кнопки More

**Crafting (2 шага):**
7. Открытие меню крафтинга
8. Объяснение системы слияния юнитов

**PvP Arena (3 шага):**
9. Возврат в More
10. Открытие PvP Arena
11. Объяснение боевой системы

**Wiki (1 шаг):**
12. Подсветка Wiki и объяснение энциклопедии

**Profile (2 шага):**
13. Открытие профиля
14. Объяснение вкладок (Inventory, Achievements, Stats, History)

**Final (1 шаг):**
15. Поздравление с завершением туториала

---

## 📁 Новые файлы

### 1. `components/TutorialOverlay.tsx`
Основной компонент туториала с:
- Затемненным фоном (85% черного)
- SVG маской с вырезом для подсветки
- Анимированной рамкой вокруг элемента
- Всплывающей подсказкой
- Блокировкой кликов (кроме подсвеченного элемента)
- Прогресс-баром (X/15)

### 2. `components/tutorialSteps.ts`
Конфигурация всех 15 шагов:
```typescript
interface TutorialStep {
  id: number;
  targetPage: string;
  targetElement?: string; // CSS selector
  position: 'top' | 'bottom' | 'left' | 'right' | 'center';
  title: string; // На русском
  description: string; // На русском
  requiresClick: boolean; // Обязательный клик
  autoNavigate?: string; // Автопереход
}
```

---

## 🔧 Изменения в существующих файлах

### App.tsx
Добавлено:
- State: `showTutorial`, `tutorialStep`
- В GameContext: `tutorialActive`, `tutorialStep`, `setActivePage`
- Выдача стартовых юнитов новым пользователям
- Проверка `tutorial_completed` из базы
- Рендер `<TutorialOverlay />` после завершения загрузки
- Блокировка daily bonus пока идет туториал

### layout/NavBar.tsx
Добавлены `data-tutorial` атрибуты:
- `data-tutorial="casino-nav"` - кнопка Casino
- `data-tutorial="wiki-nav"` - кнопка Wiki
- `data-tutorial="more-nav"` - кнопка More
- `data-tutorial="profile-nav"` - кнопка Profile

### CasinoPage.tsx
- `data-tutorial="spin-button"` на кнопке Spin

### MorePage.tsx
- `data-tutorial="crafting-button"` на кнопке Crafting
- `data-tutorial="pvp-button"` на кнопке PvP
- И другие кнопки меню

### lib/supabase.ts
Добавлено в Database типы:
```typescript
tutorial_completed: boolean | null
```

### supabase.migration.sql
Добавлено поле:
```sql
ADD COLUMN IF NOT EXISTS tutorial_completed boolean DEFAULT false;
```

---

## 🎯 Как работает туториал

### При первом входе:
1. Пользователь создается в БД с пустым инвентарем
2. Система выдает 3 стартовых юнита (Freddy, Bonnie, Chica)
3. Через 0.5 сек появляется туториал
4. `showTutorial = true`, `tutorialStep = 0`

### Во время туториала:
- Экран затемнен (85% непрозрачность)
- Подсвечен только нужный элемент
- Всплывающая подсказка с инструкцией
- Если `requiresClick: true` - пользователь ДОЛЖЕН кликнуть
- Если `requiresClick: false` - кнопка "Далее" внизу подсказки
- Автоматическое переключение страниц где нужно

### После завершения:
- В БД сохраняется `tutorial_completed = true`
- Туториал больше не показывается
- Появляется окно daily bonus

### Для существующих пользователей:
- Если `tutorial_completed = true` → туториал не показывается
- Сразу показывается daily bonus

---

## 🗄️ База данных

### Обязательно выполнить в Supabase SQL Editor:

```sql
ALTER TABLE public.profiles 
ADD COLUMN IF NOT EXISTS tutorial_completed boolean DEFAULT false;
```

Или выполните весь файл `supabase.migration.sql` заново.

---

## 🎨 Визуальные элементы

### Overlay:
- Фон: `rgba(0, 0, 0, 0.85)`
- Z-index: `10000`

### Подсветка:
- Рамка: 4px, цвет `#00ffff` (accent-cyan)
- Анимация: `animate-pulse`
- Тень: `0 0 30px rgba(0, 255, 255, 0.8)`
- Padding: 8px вокруг элемента

### Подсказка:
- Фон: `background-dark`
- Рамка: 4px, цвет `#fcee63` (accent-yellow)
- Тень: `0 0 40px rgba(252, 238, 99, 0.6)`
- Размер: 320px ширина
- Z-index: `10001`

### Прогресс-бар:
- Показывает X/15
- Желтая полоса прогресса

---

## 🐛 Важные моменты

### 1. Нельзя пропустить туториал
- Нет кнопки "Skip"
- Пользователь должен пройти все 15 шагов

### 2. Туториал показывается только 1 раз
- После завершения `tutorial_completed = true`
- Невозможно запустить повторно

### 3. Блокировка других действий
- Daily bonus не показывается пока идет туториал
- Элементы интерфейса кликабельны только если подсвечены

### 4. Автонавигация
- Туториал автоматически переключает страницы
- Пользователь не может уйти с нужной страницы

---

## 🚀 Тестирование

### Как протестировать туториал:

1. **Для нового пользователя:**
   - Зайдите с нового аккаунта Telegram
   - Туториал запустится автоматически
   - Пройдите все 15 шагов

2. **Для существующего пользователя (сброс):**
   Выполните в Supabase SQL Editor:
   ```sql
   UPDATE public.profiles 
   SET tutorial_completed = false,
       inventory = '[]'::jsonb
   WHERE id = YOUR_USER_ID;
   ```
   Затем перезагрузите приложение.

3. **Проверка стартовых юнитов:**
   После туториала в Profile → Inventory должны быть:
   - Freddy
   - Bonnie
   - Chica

---

## 📝 Текст шагов (на русском)

Все тексты находятся в `components/tutorialSteps.ts` и полностью на русском языке.

Примеры:
- "Добро пожаловать в FNTD 2 Casino!"
- "Вы получили 3 стартовых юнита!"
- "Нажмите сюда чтобы открыть казино"
- "Попробуйте прямо сейчас - нажмите Spin!"

---

## ✨ Дополнительные фичи

### Адаптивное позиционирование подсказки
Подсказка автоматически позиционируется:
- Над элементом (top)
- Под элементом (bottom)
- Слева (left)
- Справа (right)
- По центру (center)

И остается в пределах экрана (viewport).

### Стрелки-указатели
Желтые стрелки указывают от подсказки к элементу.

### Анимации
- Fade in для туториала
- Pulse для рамки подсветки
- Плавный переход между шагами

---

## 🎯 Готово!

Туториал полностью интегрирован и готов к использованию!

**Что делать дальше:**
1. Загрузите все файлы на GitHub
2. Выполните обновленный `supabase.migration.sql` в Supabase
3. Задеплойте на Vercel
4. Протестируйте с нового аккаунта!

